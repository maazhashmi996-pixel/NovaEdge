# NovaEdge — Home Page

Student GitHub collaboration project.

## Assigned page
- Student: MuhammadMuzamil-dev
- Page: Home
- Main file: `index.html`

## Files
- `index.html`
- `css/style.css`
- `js/main.js`

## Run
Open `index.html` directly in a browser, or use VS Code Live Server.

## Git workflow
Make sure your own branch is checked out before adding these files.

```bash
git branch
git status
git add .
git commit -m "Create NovaEdge home page"
git push -u origin <your-branch-name>
```

Then create a Pull Request from your branch into `main`.

## Note
The page is an original implementation inspired by the general clean, spacious,
multi-page layout of the provided reference. It does not redistribute the
reference template's source files.
